<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

	       <!--  public List<CommuteDTO> getNotices(int page, String field, String query) 
	    	int srow = 1 + (page - 1) * 5; 
			int erow = 5 + (page - 1) * 5; 
			int page => param1
			String field => param2
			String query => param3 -->
<mapper namespace="kr.or.bus.dao.CommuteDAO">
<!-- 	<select id="getSelect" resultType="kr.or.bus.dto.CommuteDTO">
	    
	   
		select * from 
					(select rownum num, N.*
					 from (
					 		select * 
					 		from commute
					 		where ${param2} like '%${param3}%' order by c_date desc
					 	   )N
					 ) where num between 1 + (${param1}-1) * 5 and 5 + (${param1}-1)*10
					  WHERE NUM BETWEEN ${param1} * ${param2} - (${param2}-1) AND ${param1} * ${param2}
	</select> -->
	
	<select id="getSelect"  resultType = "kr.or.bus.dto.CommuteDTO">
		select c_date, c_start, c_end
		from commute
		where m_id=#{m_id}
		order by c_date desc
	</select>
</mapper>














